---
import type { QaAnswer } from '../data/answers/qa-answers';
import { withBase } from '../lib/path';
import AnswerAudioPlayer from './answer/AnswerAudioPlayer.astro';
import AnswerTranscript from './answer/AnswerTranscript.astro';

interface Props {
  answer: QaAnswer;
}

const { answer } = Astro.props;
---

<article class="answer-page" data-answer-page>
  <header class="answer-page__hero answer-shell">
    <p class="answer-page__eyebrow">Alan's Economics Commons</p>
    <h1>Q&amp;A Session</h1>
    <p class="answer-page__byline">{answer.speaker}</p>
  </header>

  <section class="answer-page__question answer-shell" aria-labelledby="question-label">
    <div class="answer-page__question-card">
      <p class="answer-page__question-label" id="question-label">The question</p>
      <div class="answer-page__question-body">
        <span class="answer-page__asker-avatar" aria-hidden="true">{answer.asker.charAt(0)}</span>
        <div>
          <p class="answer-page__asker-name">{answer.asker}</p>
          <p class="answer-page__question-text">{answer.question}</p>
        </div>
      </div>
    </div>
  </section>

  <section class="answer-page__audio answer-shell" aria-label="Audio recording of Alan's answer">
    <AnswerAudioPlayer
      src={answer.audioSrc}
      type={answer.audioType}
      title={answer.question}
    />
  </section>

  {answer.comicSrc && (
    <figure class="answer-page__comic answer-shell">
      <img src={withBase(answer.comicSrc)} alt={`Illustration for: ${answer.question}`} loading="lazy" />
      {answer.comicCredit && <figcaption>{answer.comicCredit}</figcaption>}
    </figure>
  )}

  <div class="answer-shell">
    <AnswerTranscript transcript={answer.transcript} />
  </div>

  {answer.bottomImageSrc && (
    <figure class="answer-page__bottom-image answer-shell">
      <img src={withBase(answer.bottomImageSrc)} alt={`Image for: ${answer.question}`} loading="lazy" />
    </figure>
  )}
</article>

<style>
  .answer-shell {
    margin: 0 auto;
    width: min(56rem, calc(100% - 3rem));
  }
  .answer-page__hero {
    padding: clamp(4rem, 9vw, 7rem) 0 clamp(2.5rem, 5vw, 4rem);
  }
  .answer-page__eyebrow {
    color: var(--color-muted);
    font-family: var(--font-body);
    font-size: 0.68rem;
    letter-spacing: 0.14em;
    margin: 0 0 1.15rem;
    text-transform: uppercase;
  }
  .answer-page h1 {
    font-family: var(--font-display);
    font-size: clamp(2.4rem, 6.5vw, 5rem);
    font-weight: 400;
    letter-spacing: -0.05em;
    line-height: 0.95;
    margin: 0;
    max-width: 14ch;
  }
  .answer-page__byline {
    color: var(--color-muted);
    font-family: var(--font-display);
    font-size: clamp(1rem, 1.8vw, 1.25rem);
    margin: 1.4rem 0 0;
  }

  .answer-page__question {
    padding-bottom: clamp(2rem, 4vw, 3rem);
  }
  .answer-page__question-card {
    background: var(--color-paper);
    border: 1px solid var(--color-line);
    border-radius: 1rem;
    padding: 1.5rem 1.75rem;
  }
  .answer-page__question-label {
    color: var(--color-muted);
    font-family: var(--font-body);
    font-size: 0.65rem;
    letter-spacing: 0.12em;
    margin: 0 0 1rem;
    text-transform: uppercase;
  }
  .answer-page__question-body {
    align-items: start;
    display: flex;
    gap: 1rem;
  }
  .answer-page__asker-avatar {
    align-items: center;
    background: var(--color-accent);
    border-radius: 50%;
    color: var(--color-paper);
    display: flex;
    flex-shrink: 0;
    font-family: var(--font-display);
    font-size: 1.1rem;
    height: 2.5rem;
    justify-content: center;
    width: 2.5rem;
  }
  .answer-page__asker-name {
    color: var(--color-ink);
    font-family: var(--font-body);
    font-size: 0.85rem;
    font-weight: 600;
    letter-spacing: 0.04em;
    margin: 0 0 0.3rem;
    text-transform: uppercase;
  }
  .answer-page__question-text {
    color: var(--color-ink);
    font-family: var(--font-display);
    font-size: clamp(1.15rem, 2vw, 1.45rem);
    line-height: 1.4;
    margin: 0;
  }

  .answer-page__audio {
    padding-bottom: clamp(2rem, 4vw, 3rem);
  }
  .answer-page__comic {
    margin-bottom: clamp(2.5rem, 5vw, 4rem);
    margin-top: 0;
  }
  .answer-page__comic img {
    border: 1px solid var(--color-line);
    border-radius: 1rem;
    display: block;
    height: auto;
    width: 100%;
  }
  .answer-page__comic figcaption {
    color: var(--color-muted);
    font-family: var(--font-body);
    font-size: 0.72rem;
    letter-spacing: 0.04em;
    margin-top: 0.7rem;
  }
  .answer-page__bottom-image {
    margin: clamp(2.5rem, 5vw, 4rem) auto clamp(4rem, 8vw, 7rem);
  }
  .answer-page__bottom-image img {
    border: 1px solid var(--color-line);
    border-radius: 1rem;
    display: block;
    height: auto;
    width: 100%;
  }

  @media (max-width: 36rem) {
    .answer-shell { width: min(100% - 2rem, 56rem); }
    .answer-page__question-card { padding: 1.15rem; }
  }
</style>
