---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
interface Props { title?: string; description?: string; currentSection?: string; isHome?: boolean; bodyClass?: string; }
const { title = "Alan's Realm", description = 'A place for things worth wondering about.', currentSection, isHome = false, bodyClass } = Astro.props;
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <title>{title}</title>
  </head>
  <body class={bodyClass}>
    <Header currentSection={currentSection} isHome={isHome} />
    <main><slot /></main>
    <Footer />
  </body>
</html>
